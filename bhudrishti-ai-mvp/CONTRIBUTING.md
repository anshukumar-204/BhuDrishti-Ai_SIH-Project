# Contributing

Keep changes scoped to the owning module. New data layers must document source and date. Never describe demo or mapped category data as official ownership proof. Run `npm run build` in `client` and `node --check` for changed server files before opening a pull request.
