# Data pipeline

Place source GeoJSON/CSV files in `raw/`, run the processing scripts, and write normalized outputs to `processed/`. Every published layer should include source, date, CRS, and coverage metadata.
