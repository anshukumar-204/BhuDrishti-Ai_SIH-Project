# BhuDrishti AI database

The MVP currently serves traceable demo data from `server/src/data/mockData.js`. This folder is the PostgreSQL/PostGIS migration boundary for the production build.

Run `schema.sql` first, then add seed data through the migration workflow.
