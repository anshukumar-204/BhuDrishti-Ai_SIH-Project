CREATE TABLE IF NOT EXISTS users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT UNIQUE NOT NULL, role TEXT NOT NULL DEFAULT 'public', organization TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());
